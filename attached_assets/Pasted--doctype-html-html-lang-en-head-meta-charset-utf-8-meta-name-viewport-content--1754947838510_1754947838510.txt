<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Catalog Pilot – BigCommerce Product Management</title>
  <meta name="description" content="Catalog Pilot connects to BigCommerce to sync product catalogs, bulk-update pricing with work orders, track price history, and collaborate across teams." />
  <meta property="og:title" content="Catalog Pilot – BigCommerce Product Management" />
  <meta property="og:description" content="Sync. Update. Collaborate. Take control of your BigCommerce product catalog." />
  <meta property="og:type" content="website" />
  <meta name="theme-color" content="#6792FF" />
  <style>
    /* -------------------- Design Tokens -------------------- */
    :root{
      --cp-blue:#6792FF;
      --cp-coral:#FD7572;
      --cp-green:#53E590;

      --bg: #0b0d12;           /* dark default */
      --panel:#121622;
      --text:#e8e9ef;
      --muted:#b7bdd6;
      --hair:#222738;

      --brand: var(--cp-blue);
      --ok: var(--cp-green);
      --warn: var(--cp-coral);
      --shadow: 0 10px 30px rgba(0,0,0,.35);
      --radius: 14px;
      --radius-sm:10px;
    }
    /* Light theme */
    :root[data-theme="light"]{
      --bg:#f7f8fc;
      --panel:#ffffff;
      --text:#101322;
      --muted:#5a627a;
      --hair:#e7e9f3;
      --shadow: 0 8px 24px rgba(20,24,40,.08);
    }

    /* -------------------- Reset / Base -------------------- */
    *,*::before,*::after{ box-sizing: border-box; }
    html,body{ height:100%; }
    body{
      margin:0; font: 16px/1.5 system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji";
      background: radial-gradient(1200px 600px at 80% -10%, rgba(103,146,255,.15), transparent 60%),
                  radial-gradient(1000px 500px at -10% 10%, rgba(83,229,144,.12), transparent 60%),
                  var(--bg);
      color:var(--text);
      -webkit-font-smoothing: antialiased;
      text-rendering: optimizeLegibility;
    }
    a{ color: inherit; text-decoration: none; }
    img{ max-width: 100%; height:auto; }
    .container{ width:min(1120px, 92%); margin-inline:auto; }
    .grid{ display:grid; gap:24px; }
    .btn{
      display:inline-flex; align-items:center; gap:.6rem;
      padding:.85rem 1.15rem; border-radius:999px; border:1px solid transparent;
      background:linear-gradient(180deg, var(--brand), #4e79ff);
      color:#fff; font-weight:700; letter-spacing:.2px;
      box-shadow: var(--shadow);
      transition: transform .08s ease, box-shadow .2s ease, opacity .2s ease;
    }
    .btn:focus-visible{ outline:3px solid color-mix(in srgb, var(--brand) 60%, white); outline-offset:3px; }
    .btn:hover{ transform: translateY(-1px); }
    .btn:active{ transform: translateY(0); }

    header{
      position:sticky; top:0; backdrop-filter:saturate(1.1) blur(8px);
      background: color-mix(in srgb, var(--bg) 80%, transparent);
      border-bottom:1px solid var(--hair); z-index:10;
    }
    nav{ display:flex; align-items:center; justify-content:space-between; padding:.9rem 0; }
    .brand{ display:flex; align-items:center; gap:.75rem; font-weight:800; letter-spacing:.3px; }
    .brand-badge{
      width:36px; height:36px; border-radius:10px; display:grid; place-items:center;
      background: conic-gradient(from 210deg at 50% 50%, var(--cp-blue), var(--cp-green), var(--cp-coral));
      box-shadow: var(--shadow);
      color:#0a0c14; font-weight:900;
    }
    .nav-links{ display:flex; gap:18px; align-items:center; }
    .login{
      padding:.6rem .9rem; border-radius:999px; border:1px solid var(--hair);
      background: color-mix(in srgb, var(--panel) 88%, transparent);
      color:var(--text); font-weight:600;
    }
    .theme-toggle{
      border:1px solid var(--hair); border-radius:999px; background:transparent; color:var(--muted);
      padding:.55rem .75rem; cursor:pointer;
    }
    .theme-toggle:focus-visible{ outline:3px solid var(--brand); outline-offset:2px; }

    /* Hero */
    .hero{ padding: clamp(3rem, 8vw, 6rem) 0 2rem; }
    .hero-wrap{ display:grid; grid-template-columns: 1.1fr .9fr; gap:32px; align-items:center; }
    @media (max-width: 900px){ .hero-wrap{ grid-template-columns: 1fr; } }
    .eyebrow{
      display:inline-flex; gap:.5rem; align-items:center; font-weight:700;
      padding:.35rem .65rem; border-radius:999px; border:1px solid var(--hair); color:var(--muted);
      background: color-mix(in srgb, var(--panel) 92%, transparent);
    }
    h1{ font-size: clamp(2rem, 4.5vw, 3.3rem); line-height:1.15; margin:.75rem 0 1rem; }
    .lead{ color:var(--muted); font-size: clamp(1rem, 1.5vw, 1.125rem); max-width: 56ch; }
    .hero-card{
      background: linear-gradient(180deg, color-mix(in srgb, var(--panel) 86%, transparent), var(--panel));
      border:1px solid var(--hair); border-radius: var(--radius);
      padding:20px; box-shadow: var(--shadow);
    }
    .hero-metric{
      display:flex; align-items:center; gap:12px; padding:12px; border-radius:12px; border:1px dashed var(--hair);
      background: color-mix(in srgb, var(--panel) 90%, transparent);
    }
    .chip{ font-size:.85rem; color:var(--muted) }
    .mono{ font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace; }

    /* Section shells */
    section{ padding: 3.5rem 0; }
    .section-head{ display:flex; justify-content:space-between; gap:18px; align-items:end; margin-bottom:1.25rem; }
    .section-head h2{ margin:0; font-size: clamp(1.4rem, 2.6vw, 1.9rem); }
    .sub{ color:var(--muted); max-width:72ch; }

    /* Features */
    .cards{ display:grid; grid-template-columns: repeat(3,1fr); gap:18px; }
    @media (max-width: 920px){ .cards{ grid-template-columns: 1fr 1fr; } }
    @media (max-width: 640px){ .cards{ grid-template-columns: 1fr; } }
    .card{
      background: var(--panel); border:1px solid var(--hair); border-radius: var(--radius-sm);
      padding:18px; box-shadow: var(--shadow); display:flex; gap:14px; align-items:flex-start;
    }
    .icon{
      width:40px; height:40px; border-radius:10px; display:grid; place-items:center; flex:0 0 auto;
      background: color-mix(in srgb, var(--brand) 18%, transparent);
      border:1px solid color-mix(in srgb, var(--brand) 35%, var(--hair));
    }
    .card h3{ margin:.2rem 0 .3rem; font-size:1.05rem; }
    .card p{ margin:0; color:var(--muted); }

    /* How it works */
    .steps{ display:grid; grid-template-columns: repeat(3,1fr); gap:18px; counter-reset: step; }
    @media (max-width: 920px){ .steps{ grid-template-columns:1fr; } }
    .step{ background: var(--panel); border:1px solid var(--hair); border-radius: var(--radius-sm); padding:18px; position:relative; }
    .step::before{
      counter-increment: step; content: counter(step);
      position:absolute; top:-12px; left:-12px; width:36px; height:36px; border-radius:50%;
      background: var(--brand); color:#fff; display:grid; place-items:center; font-weight:800; box-shadow: var(--shadow);
    }

    /* Pricing */
    .pricing{ display:grid; grid-template-columns: repeat(3,1fr); gap:18px; }
    @media (max-width: 920px){ .pricing{ grid-template-columns:1fr; } }
    .plan{ background: var(--panel); border:1px solid var(--hair); border-radius: var(--radius); padding:20px; box-shadow: var(--shadow); }
    .plan h3{ margin:.2rem 0 .1rem; }
    .price{ font-size:1.8rem; font-weight:900; margin:.3rem 0 .6rem; }
    .pill{ display:inline-block; font-size:.85rem; color:var(--muted); border:1px solid var(--hair); border-radius:999px; padding:.18rem .55rem; }

    /* Footer */
    footer{ border-top:1px solid var(--hair); padding:2.2rem 0 3rem; color:var(--muted); }
    .links{ display:flex; flex-wrap:wrap; gap:18px; }
    .small{ font-size:.92rem; color:var(--muted); }

    /* Focus styles for accessibility */
    a:focus-visible, button:focus-visible{ outline:3px solid var(--brand); outline-offset:2px; border-radius:6px; }

    /* Skip link */
    .skip{ position:absolute; left:-9999px; top:auto; width:1px; height:1px; overflow:hidden; }
    .skip:focus{ position:static; width:auto; height:auto; padding:.5rem; background:#fff; color:#000; z-index:9999; }
  </style>
</head>
<body>
  <a class="skip" href="#main">Skip to content</a>

  <!-- Header -->
  <header>
    <div class="container">
      <nav aria-label="Primary">
        <div class="brand">
          <div class="brand-badge" aria-hidden="true">CP</div>
          <span>Catalog Pilot</span>
        </div>
        <div class="nav-links">
          <a href="#features" class="small">Features</a>
          <a href="#how" class="small">How it works</a>
          <a href="#pricing" class="small">Pricing</a>
          <a href="#faq" class="small">FAQ</a>
          <a href="/login" class="login" aria-label="Login to Catalog Pilot">Login</a>
          <button class="theme-toggle" type="button" id="themeToggle" aria-label="Toggle theme">☾</button>
        </div>
      </nav>
    </div>
  </header>

  <main id="main">
    <!-- Hero -->
    <section class="hero container" id="home">
      <div class="hero-wrap">
        <div>
          <span class="eyebrow">
            <svg width="18" height="18" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M12 2l3 7h7l-5.5 4 2.5 7-7-4.5L5 20l2.5-7L2 9h7z"/></svg>
            Navigate your catalog with confidence
          </span>
          <h1>Efficiency, control, and teamwork for <span class="mono">BigCommerce</span> product ops.</h1>
          <p class="lead">Catalog Pilot syncs your store via API, schedules bulk price updates with <strong>Work Orders</strong>, tracks pricing history, and supports multi-user company accounts—covering both standard products and variations.</p>
          <!-- Single CTA on the page -->
          <p style="margin:1.25rem 0 0;">
            <a class="btn" href="/get-started" aria-label="Get started with Catalog Pilot">
              Get Started
              <svg width="18" height="18" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M13 5l7 7-7 7v-4H4v-6h9V5z"/></svg>
            </a>
          </p>
          <p class="chip" style="margin:.8rem 0 0;">No credit card required · 14‑day Trial</p>
        </div>

        <!-- Right: Visual summary card -->
        <aside class="hero-card" aria-label="Highlights">
          <div class="grid" style="gap:12px;">
            <div class="hero-metric">
              <div class="icon" aria-hidden="true">🔗</div>
              <div>
                <strong>BigCommerce API Sync</strong><br/>
                <span class="chip">Real-time product + variant updates</span>
              </div>
            </div>

            <div class="hero-metric">
              <div class="icon" aria-hidden="true">🧭</div>
              <div>
                <strong>Work Orders</strong><br/>
                <span class="chip">Schedule bulk price changes safely</span>
              </div>
            </div>

            <div class="hero-metric">
              <div class="icon" aria-hidden="true">📈</div>
              <div>
                <strong>Price History</strong><br/>
                <span class="chip">Track trends & accountability</span>
              </div>
            </div>

            <div class="hero-metric">
              <div class="icon" aria-hidden="true">👥</div>
              <div>
                <strong>Teams & Roles</strong><br/>
                <span class="chip">Multi-user companies with permissions</span>
              </div>
            </div>
          </div>
        </aside>
      </div>
    </section>

    <!-- Feature grid -->
    <section id="features">
      <div class="container">
        <div class="section-head">
          <h2>What you can do with Catalog Pilot</h2>
          <p class="sub">Everything you need to manage catalogs at scale—without spreadsheets and late-night price edits.</p>
        </div>
        <div class="cards">
          <article class="card">
            <div class="icon" aria-hidden="true">🔄</div>
            <div>
              <h3>Sync & Normalize</h3>
              <p>Connect your BigCommerce store via API to pull products, variants, and pricing into a consistent workspace.</p>
            </div>
          </article>
          <article class="card">
            <div class="icon" aria-hidden="true">🗂️</div>
            <div>
              <h3>Catalog Browsing</h3>
              <p>Filter by brand, category, SKU, price rules, and change history. Save custom views for your team.</p>
            </div>
          </article>
          <article class="card">
            <div class="icon" aria-hidden="true">📅</div>
            <div>
              <h3>Scheduled Work Orders</h3>
              <p>Queue and approve bulk price updates with guardrails, approvals, and audit logs.</p>
            </div>
          </article>
          <article class="card">
            <div class="icon" aria-hidden="true">🧮</div>
            <div>
              <h3>Variant‑level Pricing</h3>
              <p>Update individual variant prices, compare against MSRP/MAP, and apply differential rules.</p>
            </div>
          </article>
          <article class="card">
            <div class="icon" aria-hidden="true">🧑‍🤝‍🧑</div>
            <div>
              <h3>Teams & Permissions</h3>
              <p>Invite teammates, assign roles (Admin/Editor/Viewer), and restrict sensitive actions.</p>
            </div>
          </article>
          <article class="card">
            <div class="icon" aria-hidden="true">🛡️</div>
            <div>
              <h3>Audit & Rollback</h3>
              <p>Price history with user attribution and easy rollbacks if something doesn’t look right.</p>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- How it works -->
    <section id="how">
      <div class="container">
        <div class="section-head">
          <h2>How it works</h2>
          <p class="sub">From connection to collaboration in minutes.</p>
        </div>
        <div class="steps">
          <div class="step">
            <h3>Connect your store</h3>
            <p>Authenticate with BigCommerce and select which catalogs to manage. We auto‑map products and variants.</p>
          </div>
          <div class="step">
            <h3>Plan updates</h3>
            <p>Create a Work Order for price changes, choose products/variants, schedule, and set approvers.</p>
          </div>
          <div class="step">
            <h3>Track & improve</h3>
            <p>Monitor execution, view pricing history, and refine rules—without losing control or visibility.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Pricing -->
    <section id="pricing">
      <div class="container">
        <div class="section-head">
          <h2>Simple plans that scale</h2>
          <p class="sub">Subscription tiers with product limits. Upgrade as your catalog grows.</p>
        </div>
        <div class="pricing">
          <div class="plan" aria-label="Trial Plan">
            <span class="pill">Trial</span>
            <h3>Kick the tires</h3>
            <p class="price">$0</p>
            <ul class="small">
              <li>Up to 250 products</li>
              <li>Core sync & browsing</li>
              <li>Single user</li>
              <li>Read‑only price history</li>
            </ul>
          </div>
          <div class="plan" aria-label="Starter Plan">
            <span class="pill">Starter</span>
            <h3>Growing teams</h3>
            <p class="price">$79/mo</p>
            <ul class="small">
              <li>Up to 5,000 products</li>
              <li>Work Orders + scheduling</li>
              <li>3 users, basic roles</li>
              <li>Full price history</li>
            </ul>
          </div>
          <div class="plan" aria-label="Premium Plan">
            <span class="pill">Premium</span>
            <h3>Serious scale</h3>
            <p class="price">$199/mo</p>
            <ul class="small">
              <li>50,000+ products</li>
              <li>Advanced approvals</li>
              <li>Unlimited users</li>
              <li>Priority support & SSO</li>
            </ul>
          </div>
        </div>
        <!-- (Intentionally no additional CTA buttons here to preserve single CTA requirement) -->
      </div>
    </section>

    <!-- FAQ -->
    <section id="faq">
      <div class="container">
        <div class="section-head">
          <h2>Frequently asked questions</h2>
        </div>
        <details class="plan" style="margin-bottom:12px;">
          <summary><strong>Do I need a developer to connect my store?</strong></summary>
          <p class="small">No. OAuth login with BigCommerce gets you connected in a few clicks.</p>
        </details>
        <details class="plan" style="margin-bottom:12px;">
          <summary><strong>Can I test bulk price updates safely?</strong></summary>
          <p class="small">Yes. Work Orders support approvals and scheduled execution with rollback.</p>
        </details>
        <details class="plan">
          <summary><strong>Does Catalog Pilot support variant‑level pricing?</strong></summary>
          <p class="small">Absolutely—variants can be targeted individually or via rules.</p>
        </details>

        <!-- SEO structured data for FAQ -->
        <script type="application/ld+json">
        {
          "@context":"https://schema.org",
          "@type":"FAQPage",
          "mainEntity":[
            {
              "@type":"Question",
              "name":"Do I need a developer to connect my store?",
              "acceptedAnswer":{"@type":"Answer","text":"No. OAuth login with BigCommerce gets you connected in a few clicks."}
            },
            {
              "@type":"Question",
              "name":"Can I test bulk price updates safely?",
              "acceptedAnswer":{"@type":"Answer","text":"Yes. Work Orders support approvals and scheduled execution with rollback."}
            },
            {
              "@type":"Question",
              "name":"Does Catalog Pilot support variant-level pricing?",
              "acceptedAnswer":{"@type":"Answer","text":"Absolutely—variants can be targeted individually or via rules."}
            }
          ]
        }
        </script>
      </div>
    </section>
  </main>

  <footer>
    <div class="container grid" style="gap:8px;">
      <div class="links small">
        <a href="#features">Features</a>
        <a href="#how">How it works</a>
        <a href="#pricing">Pricing</a>
        <a href="#faq">FAQ</a>
        <a href="/privacy">Privacy</a>
        <a href="/terms">Terms</a>
        <span>·</span>
        <span>© <span id="year"></span> Catalog Pilot</span>
      </div>
    </div>
  </footer>

  <script>
    // Light/Dark theme toggle with localStorage
    (function(){
      const root = document.documentElement;
      const key = 'cp-theme';
      const toggle = document.getElementById('themeToggle');

      const prefersLight = window.matchMedia('(prefers-color-scheme: light)').matches;
      const saved = localStorage.getItem(key);
      const startTheme = saved || (prefersLight ? 'light' : 'dark');
      root.setAttribute('data-theme', startTheme);
      toggle.textContent = startTheme === 'light' ? '☀︎' : '☾';

      toggle.addEventListener('click', () => {
        const next = root.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
        root.setAttribute('data-theme', next);
        localStorage.setItem(key, next);
        toggle.textContent = next === 'light' ? '☀︎' : '☾';
      });

      document.getElementById('year').textContent = new Date().getFullYear();
    })();
  </script>
</body>
</html>